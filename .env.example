# Alamat backend Flask Mintif (yang punya endpoint /api/chat dan /api/chat/stream).
# Lokal (uji di HP yang sama): http://localhost:5000
# Demo codespace: https://xxx-5000.app.github.dev (URL forwarded port backend)
# Produksi: https://mintif.domain (VPS + HTTPS)
VITE_MINTIF_API_URL=
